package org.jellyfin.mobile.player.ui.playermenuhelper

// This is just a holder class for each of the sub-helpers
class PlayerMenuHelper(
    val skipMediaSegmentButton: SkipMediaSegmentButton,
    val chapterMarkings: ChapterMarkings = ChapterMarkings(),
)
